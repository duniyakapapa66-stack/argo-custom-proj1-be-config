apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: app1-ns
spec:
  type: ClusterIP
  selector:
    app: simple-backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000 

  